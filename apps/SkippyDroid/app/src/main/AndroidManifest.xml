<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!--
      Package visibility (Android 11+). SkippyDroid resolves SkippyChat's
      launch intent when the Captain rotates to portrait — see the
      rotation-handoff block in MainActivity.kt. Without this declaration
      `packageManager.getLaunchIntentForPackage("com.skippy.chat")` returns
      null even when SkippyChat is installed.
    -->
    <queries>
        <package android:name="com.skippy.chat" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_launcher"
        android:label="SkippyDroid"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkippyDroid"
        tools:targetApi="31">

        <!--
          Orientation is `sensor` rather than `landscape` so rotating the
          phone to portrait is caught by `onConfigurationChanged` and used
          as the signal to hand off to SkippyChat. HUD content is still
          16:10 (the Compositor clamps itself); in portrait it would
          letterbox heavily, but we never render like that because the
          handoff fires first.

          `launchMode=singleTask` so SkippyChat's reverse handoff brings
          the existing SkippyDroid task back to front rather than stacking
          a fresh instance — expensive state (camera, voice, GPS, passthrough
          server) stays warm across rotations.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="sensor"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".features.notifications.NotificationListener"
            android:exported="true"
            android:label="SkippyDroid Notifications"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

    </application>

</manifest>
